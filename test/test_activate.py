import os

import pytest

from venv_manager import VenvManager


# def test_activate():
#     venv_mgr = VenvManager()
#     venv_mgr.activate()
#     prompt = os.getenv("PROMPT")
#     #assert prompt.startswith("(umuenv)")
# 
#     venv_mgr.deactivate()
#     assert not os.getenv("PROMPT").startswith("(umuenv)")
